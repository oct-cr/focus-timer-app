language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm test
  - npm run build
after_success: 
  - curl --ftp-create-dirs 
        -T ./build/
        ftp://${SFTP_USER}:${SFTP_PASSWORD}@crearlink.com/ 
